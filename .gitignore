# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
ml/.venv/
ml/**/__pycache__/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but DO commit the template of variable names (no secret values).
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tool output / caches (not source)
/graphify-out/
/.planning/
/.agents/
/.codex/

# stray nested create-next-app scaffold (duplicate of root app)
/trashium-app/

# academic deliverables (large binaries, not app source)
*.pdf

# blog showcase â€” raw source media only; processed assets ship under public/blog/**
/Dependencies/

# Claude Code / Cowork local tooling â€” local settings + installed skills, not app source
# (also avoids committing .claude/skills/karpathy-guidelines/SKILL.md, which is currently
#  throwing a filesystem I/O error on this drive)
.claude/settings.local.json
.claude/skills/

# --- deployment cleanup additions -------------------------------------------
# Local archive of planning/handoff/spec docs & tool output (kept locally, never shipped)
/_archive/

# Local full-repo backup zips
Trashium_backup_*.zip
/*.zip

# Stray local agent / tool state
/.remember/
/.impeccable/

# Tool output & lockfiles (not app source)
repomix-output.xml
skills-lock.json
.graphifyignore

# Extracted media / logs (regenerable, not source)
/extracted_images/
extracted_text.txt
debug.log

# Academic deliverables & raw media (not app source - un-ignore if you want them in the repo)
/Presentation/
/Carousel/
# NOTE: /Team/ is NOT ignored — components/layout/footer.tsx imports these photos,
# so they must ship in the repo for the Vercel (Linux) build to resolve them.

# Large trained model artifacts (kept locally + in backup; API only needs lr_mv.joblib)
ml/artifacts/rf_v1.joblib
ml/artifacts/rf_mv.joblib

# stray zip temp files
zi??????
zip_errors.log
